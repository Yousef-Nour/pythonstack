pip list // The pip list command returns the list of all frameworks(packages) in the current environment.
deactivate //deactivate the enviroment and return to the main command display
pip3 list // the number of packages when i deactivate my environment will be less.
pip install django==2.2.4 // install the virsion 2.2.4 django package
pip freeze // will display the moudules you instlled by pip untill now
pip freeze > requirements.txt // create file on the assigned location then prnt the freeze list packages insikde 
                                the text document.

pip uninstall Django// uninstall the django package
pip show django // show if the django is installed!

